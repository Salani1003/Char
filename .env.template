# En dev, cambiar a True a mano. El default es False para que un .env
# copiado sin pensar no termine sirviendo con DEBUG activo en producción.
DEBUG=False
SECRET_KEY=

ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000

DB_NAME=char
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Hardening de producción (sólo se aplican con DEBUG=False, ver config/settings.py)
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=2592000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=False
CSRF_TRUSTED_ORIGINS=